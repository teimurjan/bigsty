interface Urls {
  users: string;
  login: string;
  register: string;
  groups: string;
}

const urls: Urls = {
  users: 'api/users',
  login: 'api/login',
  register: 'api/register',
  groups: 'api/groups',
};

export default urls;